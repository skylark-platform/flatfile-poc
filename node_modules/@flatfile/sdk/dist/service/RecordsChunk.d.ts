import { ImportSession } from '../importer/ImportSession';
import { ERecordStatus, FlatfileRecord } from './FlatfileRecord';
export declare class RecordsChunk {
    private session;
    readonly records: FlatfileRecord[];
    private meta;
    constructor(session: ImportSession, records: FlatfileRecord[], meta: RecordChunkMeta);
    get recordIds(): number[];
    /**
     * Get the next chunk of data based on the current chunk
     */
    getNextChunk(): Promise<RecordsChunk | null>;
    /**
     * How many chunks are there to process in total
     */
    get totalChunks(): number;
    /**
     * Which chunk is this? (0 indexed)
     */
    get currentChunkIndex(): number;
}
export declare type RecordChunkMeta = {
    status: ERecordStatus;
    skip: number;
    totalRecords: number;
    limit: number;
    index?: number;
};
