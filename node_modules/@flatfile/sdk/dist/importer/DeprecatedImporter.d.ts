import { Flatfile } from '../Flatfile';
import { TypedEventManager } from '../lib/TypedEventManager';
import { IEvents } from '../types';
export declare class DeprecatedImporter extends TypedEventManager<IEvents> {
    ff: Flatfile;
    /**
     * Reference to instance of initialized importer
     * @private
     */
    private importer?;
    constructor(ff: Flatfile);
    /**
     * @deprecated use Flatfile.initializeFromRawToken instead
     * @param payload
     */
    __unsafeGenerateToken(payload: {
        embedId: string;
        endUserEmail: string;
        privateKey: string;
    }): Promise<void>;
    /**
     * @deprecated use flatfile.startOrResumeImport({open: 'iframe'})
     */
    launch(mode?: 'iframe' | 'window'): Promise<{
        batchId: string;
    }>;
    /**
     * @deprecated use importer.iframe.close()
     */
    close(): void;
}
