import { purry } from './purry';
export function last() {
    return purry(_last, arguments, last.lazy);
}
function _last(array) {
    return array[array.length - 1];
}
(function (last) {
    function lazy() {
        return function (value) {
            return {
                done: true,
                hasNext: true,
                next: value,
            };
        };
    }
    last.lazy = lazy;
    (function (lazy) {
        lazy.single = true;
    })(lazy = last.lazy || (last.lazy = {}));
})(last || (last = {}));
